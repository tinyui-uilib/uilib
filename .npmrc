# Route @uilib scoped packages to GitHub Packages registry
@uilib:registry=https://npm.pkg.github.com

# Auth token — read from environment variable, never hardcoded
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
